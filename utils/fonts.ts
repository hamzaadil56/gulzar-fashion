import { Ovo, Mulish } from "next/font/google";

export const titleFont = Ovo({
  weight: ["400"],
  subsets: ["latin"],
});

export const textFont = Mulish({
  weight: ["400", "500", "600", "700"],
  subsets: ["latin"],
});
